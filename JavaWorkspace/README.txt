This is the JAVA environment workspace.

Within this workspace, there are a number of key directories/files.
 - lib : any local jars will be stored in the lib folder for easy access from the projects.
 - config : any project global files (ex. db.properties).
 - tomcat6 : the location of the tomcat install for any web-based projects.

